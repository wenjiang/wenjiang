使用该程序的时候，先将图片（命名为source)放在与bin同目录下的新建文件夹文件夹里，然后运行ImageHandler。可以尝试欺骗程序，不如说，一开始运行的时候，显示的是红桃，可以输入“Brick"(方砖),再次运行程序就会发现程序输出是方块了。这里有四个txt文件夹，Brick(方砖),RedPitch(红桃),BlackPitch(黑桃），Plum(梅花),里面存储的是相应的误判的图片的指纹，这样在下次判断的时候，就能先对相应的花色和数字的指纹库进行搜索，提高识别度。但是，如果我们恶意欺骗它，它也会乖乖上当。
如果正确的话，请输入“Y”。然后按回车键。